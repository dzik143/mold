use64
movzx eax, ax
movzx eax, cx
movzx eax, dx
movzx eax, bx
movzx eax, sp
movzx eax, bp
movzx eax, si
movzx eax, di
movzx ecx, ax
movzx ecx, cx
movzx ecx, dx
movzx ecx, bx
movzx ecx, sp
movzx ecx, bp
movzx ecx, si
movzx ecx, di
movzx edx, ax
movzx edx, cx
movzx edx, dx
movzx edx, bx
movzx edx, sp
movzx edx, bp
movzx edx, si
movzx edx, di
movzx ebx, ax
movzx ebx, cx
movzx ebx, dx
movzx ebx, bx
movzx ebx, sp
movzx ebx, bp
movzx ebx, si
movzx ebx, di
movzx esp, ax
movzx esp, cx
movzx esp, dx
movzx esp, bx
movzx esp, sp
movzx esp, bp
movzx esp, si
movzx esp, di
movzx ebp, ax
movzx ebp, cx
movzx ebp, dx
movzx ebp, bx
movzx ebp, sp
movzx ebp, bp
movzx ebp, si
movzx ebp, di
movzx esi, ax
movzx esi, cx
movzx esi, dx
movzx esi, bx
movzx esi, sp
movzx esi, bp
movzx esi, si
movzx esi, di
movzx edi, ax
movzx edi, cx
movzx edi, dx
movzx edi, bx
movzx edi, sp
movzx edi, bp
movzx edi, si
movzx edi, di
