use64
movsx eax, al
movsx eax, cl
movsx eax, dl
movsx eax, bl
movsx eax, ah
movsx eax, ch
movsx eax, dh
movsx eax, bh
movsx ecx, al
movsx ecx, cl
movsx ecx, dl
movsx ecx, bl
movsx ecx, ah
movsx ecx, ch
movsx ecx, dh
movsx ecx, bh
movsx edx, al
movsx edx, cl
movsx edx, dl
movsx edx, bl
movsx edx, ah
movsx edx, ch
movsx edx, dh
movsx edx, bh
movsx ebx, al
movsx ebx, cl
movsx ebx, dl
movsx ebx, bl
movsx ebx, ah
movsx ebx, ch
movsx ebx, dh
movsx ebx, bh
movsx esp, al
movsx esp, cl
movsx esp, dl
movsx esp, bl
movsx esp, ah
movsx esp, ch
movsx esp, dh
movsx esp, bh
movsx ebp, al
movsx ebp, cl
movsx ebp, dl
movsx ebp, bl
movsx ebp, ah
movsx ebp, ch
movsx ebp, dh
movsx ebp, bh
movsx esi, al
movsx esi, cl
movsx esi, dl
movsx esi, bl
movsx esi, ah
movsx esi, ch
movsx esi, dh
movsx esi, bh
movsx edi, al
movsx edi, cl
movsx edi, dl
movsx edi, bl
movsx edi, ah
movsx edi, ch
movsx edi, dh
movsx edi, bh
