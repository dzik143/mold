use64
movzx ax, al
movzx ax, cl
movzx ax, dl
movzx ax, bl
movzx ax, ah
movzx ax, ch
movzx ax, dh
movzx ax, bh
movzx cx, al
movzx cx, cl
movzx cx, dl
movzx cx, bl
movzx cx, ah
movzx cx, ch
movzx cx, dh
movzx cx, bh
movzx dx, al
movzx dx, cl
movzx dx, dl
movzx dx, bl
movzx dx, ah
movzx dx, ch
movzx dx, dh
movzx dx, bh
movzx bx, al
movzx bx, cl
movzx bx, dl
movzx bx, bl
movzx bx, ah
movzx bx, ch
movzx bx, dh
movzx bx, bh
movzx sp, al
movzx sp, cl
movzx sp, dl
movzx sp, bl
movzx sp, ah
movzx sp, ch
movzx sp, dh
movzx sp, bh
movzx bp, al
movzx bp, cl
movzx bp, dl
movzx bp, bl
movzx bp, ah
movzx bp, ch
movzx bp, dh
movzx bp, bh
movzx si, al
movzx si, cl
movzx si, dl
movzx si, bl
movzx si, ah
movzx si, ch
movzx si, dh
movzx si, bh
movzx di, al
movzx di, cl
movzx di, dl
movzx di, bl
movzx di, ah
movzx di, ch
movzx di, dh
movzx di, bh
