use64
movzx rax, ax
movzx rax, cx
movzx rax, dx
movzx rax, bx
movzx rax, sp
movzx rax, bp
movzx rax, si
movzx rax, di
movzx rcx, ax
movzx rcx, cx
movzx rcx, dx
movzx rcx, bx
movzx rcx, sp
movzx rcx, bp
movzx rcx, si
movzx rcx, di
movzx rdx, ax
movzx rdx, cx
movzx rdx, dx
movzx rdx, bx
movzx rdx, sp
movzx rdx, bp
movzx rdx, si
movzx rdx, di
movzx rbx, ax
movzx rbx, cx
movzx rbx, dx
movzx rbx, bx
movzx rbx, sp
movzx rbx, bp
movzx rbx, si
movzx rbx, di
movzx rsp, ax
movzx rsp, cx
movzx rsp, dx
movzx rsp, bx
movzx rsp, sp
movzx rsp, bp
movzx rsp, si
movzx rsp, di
movzx rbp, ax
movzx rbp, cx
movzx rbp, dx
movzx rbp, bx
movzx rbp, sp
movzx rbp, bp
movzx rbp, si
movzx rbp, di
movzx rsi, ax
movzx rsi, cx
movzx rsi, dx
movzx rsi, bx
movzx rsi, sp
movzx rsi, bp
movzx rsi, si
movzx rsi, di
movzx rdi, ax
movzx rdi, cx
movzx rdi, dx
movzx rdi, bx
movzx rdi, sp
movzx rdi, bp
movzx rdi, si
movzx rdi, di
