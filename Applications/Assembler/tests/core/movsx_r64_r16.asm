use64
movsx rax, ax
movsx rax, cx
movsx rax, dx
movsx rax, bx
movsx rax, sp
movsx rax, bp
movsx rax, si
movsx rax, di
movsx rcx, ax
movsx rcx, cx
movsx rcx, dx
movsx rcx, bx
movsx rcx, sp
movsx rcx, bp
movsx rcx, si
movsx rcx, di
movsx rdx, ax
movsx rdx, cx
movsx rdx, dx
movsx rdx, bx
movsx rdx, sp
movsx rdx, bp
movsx rdx, si
movsx rdx, di
movsx rbx, ax
movsx rbx, cx
movsx rbx, dx
movsx rbx, bx
movsx rbx, sp
movsx rbx, bp
movsx rbx, si
movsx rbx, di
movsx rsp, ax
movsx rsp, cx
movsx rsp, dx
movsx rsp, bx
movsx rsp, sp
movsx rsp, bp
movsx rsp, si
movsx rsp, di
movsx rbp, ax
movsx rbp, cx
movsx rbp, dx
movsx rbp, bx
movsx rbp, sp
movsx rbp, bp
movsx rbp, si
movsx rbp, di
movsx rsi, ax
movsx rsi, cx
movsx rsi, dx
movsx rsi, bx
movsx rsi, sp
movsx rsi, bp
movsx rsi, si
movsx rsi, di
movsx rdi, ax
movsx rdi, cx
movsx rdi, dx
movsx rdi, bx
movsx rdi, sp
movsx rdi, bp
movsx rdi, si
movsx rdi, di
