use64
shl al, cl
shl cl, cl
shl dl, cl
shl bl, cl
shl ah, cl
shl ch, cl
shl dh, cl
shl bh, cl
shl ax, cl
shl cx, cl
shl dx, cl
shl bx, cl
shl sp, cl
shl bp, cl
shl si, cl
shl di, cl
shl eax, cl
shl ecx, cl
shl edx, cl
shl ebx, cl
shl esp, cl
shl ebp, cl
shl esi, cl
shl edi, cl
shl rax, cl
shl rcx, cl
shl rdx, cl
shl rbx, cl
shl rsp, cl
shl rbp, cl
shl rsi, cl
shl rdi, cl
