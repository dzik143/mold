use64
movzx eax, al
movzx eax, cl
movzx eax, dl
movzx eax, bl
movzx eax, ah
movzx eax, ch
movzx eax, dh
movzx eax, bh
movzx ecx, al
movzx ecx, cl
movzx ecx, dl
movzx ecx, bl
movzx ecx, ah
movzx ecx, ch
movzx ecx, dh
movzx ecx, bh
movzx edx, al
movzx edx, cl
movzx edx, dl
movzx edx, bl
movzx edx, ah
movzx edx, ch
movzx edx, dh
movzx edx, bh
movzx ebx, al
movzx ebx, cl
movzx ebx, dl
movzx ebx, bl
movzx ebx, ah
movzx ebx, ch
movzx ebx, dh
movzx ebx, bh
movzx esp, al
movzx esp, cl
movzx esp, dl
movzx esp, bl
movzx esp, ah
movzx esp, ch
movzx esp, dh
movzx esp, bh
movzx ebp, al
movzx ebp, cl
movzx ebp, dl
movzx ebp, bl
movzx ebp, ah
movzx ebp, ch
movzx ebp, dh
movzx ebp, bh
movzx esi, al
movzx esi, cl
movzx esi, dl
movzx esi, bl
movzx esi, ah
movzx esi, ch
movzx esi, dh
movzx esi, bh
movzx edi, al
movzx edi, cl
movzx edi, dl
movzx edi, bl
movzx edi, ah
movzx edi, ch
movzx edi, dh
movzx edi, bh
