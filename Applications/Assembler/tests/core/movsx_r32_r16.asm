use64
movsx eax, ax
movsx eax, cx
movsx eax, dx
movsx eax, bx
movsx eax, sp
movsx eax, bp
movsx eax, si
movsx eax, di
movsx ecx, ax
movsx ecx, cx
movsx ecx, dx
movsx ecx, bx
movsx ecx, sp
movsx ecx, bp
movsx ecx, si
movsx ecx, di
movsx edx, ax
movsx edx, cx
movsx edx, dx
movsx edx, bx
movsx edx, sp
movsx edx, bp
movsx edx, si
movsx edx, di
movsx ebx, ax
movsx ebx, cx
movsx ebx, dx
movsx ebx, bx
movsx ebx, sp
movsx ebx, bp
movsx ebx, si
movsx ebx, di
movsx esp, ax
movsx esp, cx
movsx esp, dx
movsx esp, bx
movsx esp, sp
movsx esp, bp
movsx esp, si
movsx esp, di
movsx ebp, ax
movsx ebp, cx
movsx ebp, dx
movsx ebp, bx
movsx ebp, sp
movsx ebp, bp
movsx ebp, si
movsx ebp, di
movsx esi, ax
movsx esi, cx
movsx esi, dx
movsx esi, bx
movsx esi, sp
movsx esi, bp
movsx esi, si
movsx esi, di
movsx edi, ax
movsx edi, cx
movsx edi, dx
movsx edi, bx
movsx edi, sp
movsx edi, bp
movsx edi, si
movsx edi, di
