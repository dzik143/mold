use64
movsx ax, al
movsx ax, cl
movsx ax, dl
movsx ax, bl
movsx ax, ah
movsx ax, ch
movsx ax, dh
movsx ax, bh
movsx cx, al
movsx cx, cl
movsx cx, dl
movsx cx, bl
movsx cx, ah
movsx cx, ch
movsx cx, dh
movsx cx, bh
movsx dx, al
movsx dx, cl
movsx dx, dl
movsx dx, bl
movsx dx, ah
movsx dx, ch
movsx dx, dh
movsx dx, bh
movsx bx, al
movsx bx, cl
movsx bx, dl
movsx bx, bl
movsx bx, ah
movsx bx, ch
movsx bx, dh
movsx bx, bh
movsx sp, al
movsx sp, cl
movsx sp, dl
movsx sp, bl
movsx sp, ah
movsx sp, ch
movsx sp, dh
movsx sp, bh
movsx bp, al
movsx bp, cl
movsx bp, dl
movsx bp, bl
movsx bp, ah
movsx bp, ch
movsx bp, dh
movsx bp, bh
movsx si, al
movsx si, cl
movsx si, dl
movsx si, bl
movsx si, ah
movsx si, ch
movsx si, dh
movsx si, bh
movsx di, al
movsx di, cl
movsx di, dl
movsx di, bl
movsx di, ah
movsx di, ch
movsx di, dh
movsx di, bh
