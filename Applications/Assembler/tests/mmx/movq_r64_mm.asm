use64
movq rax, mm0
movq rax, mm1
movq rax, mm2
movq rax, mm3
movq rax, mm4
movq rax, mm5
movq rax, mm6
movq rax, mm7
movq rcx, mm0
movq rcx, mm1
movq rcx, mm2
movq rcx, mm3
movq rcx, mm4
movq rcx, mm5
movq rcx, mm6
movq rcx, mm7
movq rdx, mm0
movq rdx, mm1
movq rdx, mm2
movq rdx, mm3
movq rdx, mm4
movq rdx, mm5
movq rdx, mm6
movq rdx, mm7
movq rbx, mm0
movq rbx, mm1
movq rbx, mm2
movq rbx, mm3
movq rbx, mm4
movq rbx, mm5
movq rbx, mm6
movq rbx, mm7
movq rsp, mm0
movq rsp, mm1
movq rsp, mm2
movq rsp, mm3
movq rsp, mm4
movq rsp, mm5
movq rsp, mm6
movq rsp, mm7
movq rbp, mm0
movq rbp, mm1
movq rbp, mm2
movq rbp, mm3
movq rbp, mm4
movq rbp, mm5
movq rbp, mm6
movq rbp, mm7
movq rsi, mm0
movq rsi, mm1
movq rsi, mm2
movq rsi, mm3
movq rsi, mm4
movq rsi, mm5
movq rsi, mm6
movq rsi, mm7
movq rdi, mm0
movq rdi, mm1
movq rdi, mm2
movq rdi, mm3
movq rdi, mm4
movq rdi, mm5
movq rdi, mm6
movq rdi, mm7
