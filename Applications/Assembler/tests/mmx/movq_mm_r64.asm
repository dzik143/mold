use64
movq mm0, rax
movq mm0, rcx
movq mm0, rdx
movq mm0, rbx
movq mm0, rsp
movq mm0, rbp
movq mm0, rsi
movq mm0, rdi
movq mm1, rax
movq mm1, rcx
movq mm1, rdx
movq mm1, rbx
movq mm1, rsp
movq mm1, rbp
movq mm1, rsi
movq mm1, rdi
movq mm2, rax
movq mm2, rcx
movq mm2, rdx
movq mm2, rbx
movq mm2, rsp
movq mm2, rbp
movq mm2, rsi
movq mm2, rdi
movq mm3, rax
movq mm3, rcx
movq mm3, rdx
movq mm3, rbx
movq mm3, rsp
movq mm3, rbp
movq mm3, rsi
movq mm3, rdi
movq mm4, rax
movq mm4, rcx
movq mm4, rdx
movq mm4, rbx
movq mm4, rsp
movq mm4, rbp
movq mm4, rsi
movq mm4, rdi
movq mm5, rax
movq mm5, rcx
movq mm5, rdx
movq mm5, rbx
movq mm5, rsp
movq mm5, rbp
movq mm5, rsi
movq mm5, rdi
movq mm6, rax
movq mm6, rcx
movq mm6, rdx
movq mm6, rbx
movq mm6, rsp
movq mm6, rbp
movq mm6, rsi
movq mm6, rdi
movq mm7, rax
movq mm7, rcx
movq mm7, rdx
movq mm7, rbx
movq mm7, rsp
movq mm7, rbp
movq mm7, rsi
movq mm7, rdi
